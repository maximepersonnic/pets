<h1><%= @pet.name %></h1>
<ul>
  <li> Found <%= @pet.found_in_days %> days ago</li>
  <li><%= @pet.species %></li>
</ul>
<%= link_to "Edit this pet", pet_edit_path(@pet) %>
<%= link_to "Delete this pet", pet_path(@pet), method: :delete, data: {confirm: "are you sure ?"} %>

<%= link_to " < back to list", pets_path %>
